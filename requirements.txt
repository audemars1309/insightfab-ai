# WhiteHouse — KLA restoration submission. Inference-only dependencies.
# Pinned to the exact versions used to produce the submitted results.
#
# run.py imports only numpy + torch. No torchvision, no lpips, no scikit/pandas,
# nothing that reaches the network at inference time.
#
# torch/torchvision wheels are CUDA-enabled by default from PyPI. To match a
# specific CUDA runtime (e.g. CUDA 11.8), install torch from the matching index:
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu118

numpy==2.4.6
torch==2.7.1
